- **[HIGH] [RELIABILITY]** [src/notifications/email.ts:170](/Users/javidrazack/pingflare/src/notifications/email.ts:170) — Docker's socket adapter returns `void` from `close()`, but email delivery calls `.catch()` on the result. A successful SMTP exchange therefore throws during cleanup and the durable outbox retries an already accepted message. Reproduced locally with synthetic SMTP responses, without sending mail. Make the adapter match the asynchronous socket contract and test the actual Node adapter.
- **[HIGH] [SECURITY]** [src/shims/cloudflare-sockets.ts:54](/Users/javidrazack/pingflare/src/shims/cloudflare-sockets.ts:54) — Both STARTTLS and implicit TLS disable certificate verification. A network attacker able to intercept the SMTP connection can impersonate the mail server and receive credentials. Enable certificate and hostname verification and carry the expected server name through STARTTLS.
- **[HIGH] [SECURITY]** [src/notifications/email.ts:70](/Users/javidrazack/pingflare/src/notifications/email.ts:70) — SMTP errors include the command's first token; during AUTH LOGIN that token is the Base64-encoded username or password. The error reaches notification-test storage and alert logging. Confirmed with a synthetic rejected-password response. Log a safe command/stage label, never authentication payloads.
- **[HIGH] [AUTH]** [src/routes/auth.ts:58](/Users/javidrazack/pingflare/src/routes/auth.ts:58) — A valid bearer token can renew itself for another 30 days; logout only clears local storage and changing ADMIN_PASS does not invalidate existing tokens. A stolen token can retain access through repeated refresh until JWT_SECRET changes. Add revocable sessions or a validated session version, bounded refresh lifetime, and server-side logout. This follows [OWASP's JWT termination guidance](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html#jwt).
- **[HIGH] [RELIABILITY]** [frontend/src/routes/(app)/+page.svelte:153](/Users/javidrazack/pingflare/frontend/src/routes/(app)/+page.svelte:153) — Dashboard health uses lastStatus without accounting for overdue checks or paused monitors. The public API also returns lastStatus directly at src/routes/publicStatus.ts:304. A stopped scheduler can leave old successes looking operational even while dashboard requests succeed. Derive freshness from the expected check schedule, expose unknown/stale separately, and exclude paused monitors from active-health totals.
- **[MEDIUM] [BUILD]** [Dockerfile:6](/Users/javidrazack/pingflare/Dockerfile:6) — The frontend image uses npm install without the root workspace lockfile, whereas CI uses npm ci. Images can resolve different dependencies from the tested checkout. Build both workspaces from the root lockfile with npm ci.
- **[MEDIUM] [PERF]** [src/routes/monitors.ts:123](/Users/javidrazack/pingflare/src/routes/monitors.ts:123) — Dashboard polling reads every monitor and serializes full configuration and metrics every 30 seconds; list pagination does not protect this path. Add a compact dashboard endpoint with aggregate counts, bounded attention results, and only fields the dashboard displays.
- **[MEDIUM] [RELIABILITY]** [src/server.ts:29](/Users/javidrazack/pingflare/src/server.ts:29) — The Docker rate-limiter Map never removes expired keys. Distinct login usernames and public visitor keys accumulate for the lifetime of the process. Add expiration cleanup and a hard capacity bound.
- **[MEDIUM] [UX]** [frontend/src/routes/s/[slug]/+page.svelte:30](/Users/javidrazack/pingflare/frontend/src/routes/s/[slug]/+page.svelte:30) — Public status loading lacks a catch/finally around fetch and JSON parsing. A network failure on initial load leaves loading true; a later failure leaves previous data without a freshness warning. Add explicit offline/error states, bounded retry, and last-successful-update information.
- **[MEDIUM] [UX]** [frontend/src/lib/api.ts:30](/Users/javidrazack/pingflare/frontend/src/lib/api.ts:30) — Shared API requests have no application deadline. A hung request holds dashboard in-flight guards or form saving states until the browser eventually fails. Add bounded, operation-appropriate timeouts and cancellation; do not automatically retry non-idempotent writes.
- **[MEDIUM] [UX]** [frontend/src/lib/components/MonitorForm.svelte:285](/Users/javidrazack/pingflare/frontend/src/lib/components/MonitorForm.svelte:285) — The form offers IPv4/IPv6 selection, but the checker never reads ipVersion. docs/API.md acknowledges it is only stored. Disable the unsupported choice with a clear explanation, or implement address-family enforcement on a capable runtime.
- **[MEDIUM] [UX]** [src/routes/monitors.ts:142](/Users/javidrazack/pingflare/src/routes/monitors.ts:142) — Search inserts backslashes before `_` and `%` but the SQL LIKE expressions have no ESCAPE clause. Literal searches such as api_prod fail to match the corresponding name; reproduced against SQLite. Escape the escape character too and declare the SQL escape convention explicitly.
- **[IMPROVEMENT] [CAPACITY]** [src/cron.ts:25](/Users/javidrazack/pingflare/src/cron.ts:25) — First product priority: expose scheduled demand, admitted throughput, total overdue monitors, oldest overdue age, and likely detection delay before users save an overloaded schedule. Current admission bounds apply to Docker as well. Keep conservative Free-plan defaults and introduce separately tested runtime capacity profiles. Cloudflare now explicitly documents daily-limit query failures, making visible quota headroom valuable: [D1 enforcement announcement](https://developers.cloudflare.com/changelog/post/2026-09-01-d1-free-tier-limit-enforcement/).
- **[IMPROVEMENT] [OPERATIONS]** [src/index.ts:86](/Users/javidrazack/pingflare/src/index.ts:86) — Add a monitoring-engine health view and a separate readiness endpoint. The existing endpoint proves the HTTP process responds, not that D1 works or checks are running. Track last completed scheduler run, lag, database failures, and an optional heartbeat sent to an independently hosted watchdog.
- **[IMPROVEMENT] [NOTIFICATIONS]** [src/db/schema.ts:157](/Users/javidrazack/pingflare/src/db/schema.ts:157) — Add a delivery inbox showing pending age, channel failures, next retry, and delivery outcome. The outbox stores useful state, but the dashboard exposes only notification test history; successful outbox rows are deleted. Retain bounded delivery receipts and offer an explicit retry action.
- **[IMPROVEMENT] [MAINTENANCE]** [src/routes/monitors.ts:517](/Users/javidrazack/pingflare/src/routes/monitors.ts:517) — Bring existing maintenance endpoints into the dashboard, then add recurring, timezone-aware schedules and bulk application by tag. The schema and API already support one-off windows, but the frontend has no maintenance management flow. This is a comparatively small feature with immediate operational value.
- **[IMPROVEMENT] [ALERTING]** [src/services/alert-manager.ts:298](/Users/javidrazack/pingflare/src/services/alert-manager.ts:298) — Add dependency-aware notifications: when a host or gateway fails, group dependent-service alerts beneath it while continuing to record each service's observations. Show the suppression reason and reject dependency cycles. This reduces alert storms more usefully than adding another notification provider.
- **[IMPROVEMENT] [INCIDENTS]** [src/db/schema.ts:190](/Users/javidrazack/pingflare/src/db/schema.ts:190) — Add acknowledge/snooze controls, runbook links, and simple escalation such as retrying through a second channel after an unacknowledged interval. Start with the single-admin workflow; team accounts and full on-call scheduling are a larger product expansion.
- **[IMPROVEMENT] [MONITORING]** [frontend/src/lib/components/MonitorForm.svelte:351](/Users/javidrazack/pingflare/frontend/src/lib/components/MonitorForm.svelte:351) — Add bounded text contains/does-not-contain assertions, numeric JSON assertions, and response-time thresholds with consecutive-breach tolerance. Existing HTTP status and JSONPath checks provide a natural foundation; a successful HTTP response alone does not prove the service is useful.
- **[IMPROVEMENT] [MONITORING]** [src/cron.ts:506](/Users/javidrazack/pingflare/src/cron.ts:506) — Add proactive certificate-expiry alerts, with selectable lead times, using a runtime/probe that can actually retrieve certificate metadata. Current SSL status is derived from request success/errors and cannot warn before expiry. Treat region-specific probes as a later expansion requiring explicit location evidence and result reconciliation.
- **[IMPROVEMENT] [ANALYTICS]** [src/services/history-rollups.ts:164](/Users/javidrazack/pingflare/src/services/history-rollups.ts:164) — Add availability targets, error-budget reporting, and observation coverage. Current uptime is the proportion of successful observations; distinguish that from time-weighted availability and explicitly represent unobserved periods before labeling reports as SLA/SLO evidence. Tail-latency reporting would require new histograms or samples, not reconstruction from averages.
- **[IMPROVEMENT] [PUBLIC STATUS]** [frontend/src/routes/s/[slug]/+page.svelte:97](/Users/javidrazack/pingflare/frontend/src/routes/s/[slug]/+page.svelte:97) — Add service groups, planned-maintenance announcements, and an RSS/Atom incident feed. Add opt-in email subscriptions later with verification and unsubscribe handling. These extend the existing published-incident workflow into useful subscriber communication.
- **[IMPROVEMENT] [BACKUP]** [src/routes/backup.ts:108](/Users/javidrazack/pingflare/src/routes/backup.ts:108) — Add a restore preview with counts, destructive effects, token-regeneration warnings, and an encryption-key compatibility check before replacement. Exported credentials remain encrypted with the source instance key; a portable passphrase-protected export or an explicit original-key restore procedure would improve recovery to a new instance.
- **[IMPROVEMENT] [TESTING]** [frontend/vitest.config.ts:7](/Users/javidrazack/pingflare/frontend/vitest.config.ts:7) — Add a small browser integration suite for login, create/edit monitor, failed refresh, maintenance, incident publishing, and protected status access, plus Node SMTP adapter tests. The frontend suite currently contains only five infrastructure-helper tests and cannot catch these interaction or runtime-adapter defects.
- **[IMPROVEMENT] [BUILD]** [package.json:30](/Users/javidrazack/pingflare/package.json:30) — Pin and document the local Node runtime consistently with CI/Docker and reinstall native dependencies when changing it. Validation here: typecheck passed with zero Svelte warnings, frontend production build and Node server build passed, frontend tests passed 5/5; backend tests passed 77/190, with 113 failing on the same better-sqlite3 ABI mismatch (installed ABI 137 versus runtime ABI 147), so database-dependent behavior was not validated. This review used source inspection and isolated local reproductions; no live deployment, real email delivery, rendered-browser review, dependency audit, or production load test was performed. Application code was not changed.
